using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Region 
{
    public float x;

    public float y;

    public Vector3 pos;

    public Region()
    {
        
    }
    public Region(float x, float y, Vector3 position)
    {
        this.x = x;
        this.y = y;
        this.pos = position;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
